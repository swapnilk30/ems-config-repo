application:
  message: Welcome From Developement Profile - auth-service

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/auth_dev
    username: root
    password: Skmania@1234
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

        
eureka:
  client:
    enabled: true
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    hostname: localhost

jwt:
  secret: myVerySecretKeyForJwtAuthentication123456789
  expiration: 3600000
