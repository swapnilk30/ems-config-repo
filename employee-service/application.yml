application:
  message: Welcome From Default Profile - employee-service
  
spring:
  application:
    name: employee-service
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}  # Default to dev
  config:
    import:
    - optional:configserver:http://localhost:8888
      
# Common configurations
server:
  port: 9093
