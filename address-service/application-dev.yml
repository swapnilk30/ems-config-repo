application:
  message: Welcome From DEV Profile - address-service

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/address_dev
    username: root
    password: Skmania@1234
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

        
eureka:
  client:
    enabled: true
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    hostname: localhost

# Feign Client Configuration (use URLs directly)
employee:
  service:
    url: http://localhost:9092
